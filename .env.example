# .NET API base URL (no trailing slash on /api segment if included in path)
VITE_API_BASE_URL=/api
VITE_APP_NAME=Prestij Tütün

# Geçici test girişi (yalnızca npm run dev — production build'de etkisiz)
VITE_DEV_AUTH_ENABLED=true
